# ─────────────────────────────── Dependencies ───────────────────────────────
node_modules/

# ─────────────────────────────── Build output ───────────────────────────────
dist/
dist-ssr/
build/
out/
.vite/
*.tsbuildinfo

# ──────────────────────────────── Rust / Tauri ──────────────────────────────
src-tauri/target/
target/
src-tauri/binaries/
src-tauri/gen/
**/*.rs.bk

# ── Compiled engine / sidecar binaries (shipped via GitHub Releases, never committed) ──
*.bun-build
*.node

# ───────────── Packaged app artifacts (delivered as Release assets, never committed) ─────────────
*.dmg
*.app/
*.app.zip
*.pkg
*.AppImage
*.msi
*.exe

# ─────────────────────── Secrets / credentials (NEVER commit) ────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keychain
*.minisign.key
*-license-private*
license.private.*

# ─────────────────────────────── Logs / caches ──────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.cache/

# ─────────────────────────────── OS / editor ────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*~

# ─────────────────────────────── decant-specific ────────────────────────────
app/public/pdf.worker.min.mjs
docs/screenshots/*.png
tests/heavy/*.pdf
tests/bench-*.json
# ── Secret/credential files — NEVER commit (see ../CLAUDE.md RULE #1) ──
.mcp.json
*.p8
AuthKey_*.p8
*-service-account*.json
*serviceAccount*.json
ga4-*.json
*.mobileprovision
*.cer
*ed25519*private*
compare.txt

# ── Generated mobile / Windows-Store icon sets — macOS DMG only, not used by the build ──
src-tauri/icons/android/
src-tauri/icons/ios/
src-tauri/icons/Square*Logo.png
src-tauri/icons/StoreLogo.png